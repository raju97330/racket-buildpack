#!/usr/bin/env bash
# bin/release <build-dir>

# Writes a default start command, but you should use a Procfile or manifest
# to override where necessary.

echo -e "---\ndefault_process_type:\n  web: ./racketapp/bin/main"
